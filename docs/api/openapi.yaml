openapi: 3.1.0
info:
  title: Campaign Organizer API
  version: 0.1.0
  description: >
    Personal worldbuilding and RPG campaign management application.
    This document is the **canonical, design-first contract** for the API.
    Both the Spring Boot backend and the React/TypeScript frontend are expected
    to conform to it (see docs/adr/0008-contract-first-openapi.md).

    Phase 0 covers single-password authentication and the `worlds` resource,
    which is the top-level container every other domain object will hang off of.
  license:
    name: Proprietary (personal use)
  contact:
    name: Campaign Organizer

servers:
  - url: /api
    description: Same-origin API root (frontend proxies /api to the backend)

tags:
  - name: auth
    description: Authentication for the single owner user.
  - name: worlds
    description: Top-level worldbuilding containers.
  - name: categories
    description: Hierarchical grouping of articles within a world.
  - name: articles
    description: Wiki articles within a world.
  - name: templates
    description: Static article template definitions and prompts.
  - name: media
    description: Uploaded images stored on the local volume.
  - name: maps
    description: Interactive maps and their article-linked pins.
  - name: timelines
    description: Timelines and their dated, article-linked events.
  - name: calendars
    description: Custom fantasy calendars with named months.
  - name: relationships
    description: Directed or undirected links between articles (relationship webs).
  - name: campaigns
    description: Gamemaster campaigns and their play sessions.
  - name: arcs
    description: Story arcs and their ordered beats within a campaign.
  - name: sheets
    description: Schema-driven character sheet templates and character sheets.
  - name: dice
    description: Stateless dice roller.
  - name: statblocks
    description: Reusable NPC/monster statblocks.
  - name: export
    description: Export a world as a JSON bundle.
  - name: whiteboards
    description: Free-form plotting whiteboards (nodes and connections).

paths:
  /auth/login:
    post:
      tags: [auth]
      summary: Exchange the app password for a bearer token
      operationId: login
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginRequest'
      responses:
        '200':
          description: Authentication succeeded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '400':
          $ref: '#/components/responses/BadRequest'

  /worlds:
    get:
      tags: [worlds]
      summary: List all worlds
      operationId: listWorlds
      responses:
        '200':
          description: All worlds owned by the user, newest first.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/World'
        '401':
          $ref: '#/components/responses/Unauthorized'
    post:
      tags: [worlds]
      summary: Create a world
      operationId: createWorld
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WorldRequest'
      responses:
        '201':
          description: World created.
          headers:
            Location:
              description: URI of the created world.
              schema:
                type: string
                format: uri
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/World'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /worlds/{worldId}:
    parameters:
      - $ref: '#/components/parameters/WorldId'
    get:
      tags: [worlds]
      summary: Get a single world
      operationId: getWorld
      responses:
        '200':
          description: The world.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/World'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      tags: [worlds]
      summary: Update a world
      operationId: updateWorld
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WorldRequest'
      responses:
        '200':
          description: World updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/World'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags: [worlds]
      summary: Delete a world
      operationId: deleteWorld
      responses:
        '204':
          description: World deleted.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /article-templates:
    get:
      tags: [templates]
      summary: List article template definitions and their prompts
      operationId: listArticleTemplates
      responses:
        '200':
          description: All template definitions.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ArticleTemplateInfo'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /worlds/{worldId}/media:
    parameters:
      - $ref: '#/components/parameters/WorldId'
    get:
      tags: [media]
      summary: List a world's media assets
      operationId: listMedia
      responses:
        '200':
          description: Media assets, newest first.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/MediaAsset'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      tags: [media]
      summary: Upload an image
      operationId: uploadMedia
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '201':
          description: Media uploaded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MediaAsset'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/media/{mediaId}:
    parameters:
      - $ref: '#/components/parameters/WorldId'
      - $ref: '#/components/parameters/MediaId'
    delete:
      tags: [media]
      summary: Delete a media asset
      operationId: deleteMedia
      responses:
        '204':
          description: Media deleted.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /media/{mediaId}/content:
    parameters:
      - $ref: '#/components/parameters/MediaId'
    get:
      tags: [media]
      summary: Serve raw image bytes (public, addressed by unguessable id)
      operationId: getMediaContent
      security: []
      responses:
        '200':
          description: The image bytes.
          content:
            image/*:
              schema:
                type: string
                format: binary
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/maps:
    parameters:
      - $ref: '#/components/parameters/WorldId'
    get:
      tags: [maps]
      summary: List a world's maps
      operationId: listMaps
      responses:
        '200':
          description: Maps in the world, newest first.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Map'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      tags: [maps]
      summary: Create a map from an uploaded image
      operationId: createMap
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MapRequest'
      responses:
        '201':
          description: Map created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Map'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/maps/{mapId}:
    parameters:
      - $ref: '#/components/parameters/WorldId'
      - $ref: '#/components/parameters/MapId'
    get:
      tags: [maps]
      summary: Get a map
      operationId: getMap
      responses:
        '200':
          description: The map.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Map'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      tags: [maps]
      summary: Update a map
      operationId: updateMap
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MapRequest'
      responses:
        '200':
          description: Map updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Map'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags: [maps]
      summary: Delete a map (and its pins)
      operationId: deleteMap
      responses:
        '204':
          description: Map deleted.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/maps/{mapId}/pins:
    parameters:
      - $ref: '#/components/parameters/WorldId'
      - $ref: '#/components/parameters/MapId'
    get:
      tags: [maps]
      summary: List a map's pins
      operationId: listPins
      responses:
        '200':
          description: Pins on the map.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/MapPin'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      tags: [maps]
      summary: Add a pin to a map
      operationId: createPin
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MapPinRequest'
      responses:
        '201':
          description: Pin created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MapPin'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/maps/{mapId}/pins/{pinId}:
    parameters:
      - $ref: '#/components/parameters/WorldId'
      - $ref: '#/components/parameters/MapId'
      - $ref: '#/components/parameters/PinId'
    put:
      tags: [maps]
      summary: Update a pin
      operationId: updatePin
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MapPinRequest'
      responses:
        '200':
          description: Pin updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MapPin'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags: [maps]
      summary: Delete a pin
      operationId: deletePin
      responses:
        '204':
          description: Pin deleted.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/relationships:
    parameters:
      - $ref: '#/components/parameters/WorldId'
    get:
      tags: [relationships]
      summary: List all relationships in a world (the full graph)
      operationId: listRelationships
      responses:
        '200':
          description: All relationship edges in the world.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Relationship'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      tags: [relationships]
      summary: Create a relationship between two articles
      operationId: createRelationship
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RelationshipRequest'
      responses:
        '201':
          description: Relationship created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Relationship'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/relationships/{relationshipId}:
    parameters:
      - $ref: '#/components/parameters/WorldId'
      - $ref: '#/components/parameters/RelationshipId'
    put:
      tags: [relationships]
      summary: Update a relationship
      operationId: updateRelationship
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RelationshipRequest'
      responses:
        '200':
          description: Relationship updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Relationship'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags: [relationships]
      summary: Delete a relationship
      operationId: deleteRelationship
      responses:
        '204':
          description: Relationship deleted.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/articles/{articleId}/relationships:
    parameters:
      - $ref: '#/components/parameters/WorldId'
      - $ref: '#/components/parameters/ArticleId'
    get:
      tags: [relationships]
      summary: List relationships touching one article (its neighbourhood)
      operationId: listArticleRelationships
      responses:
        '200':
          description: Edges where the article is either endpoint.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Relationship'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/calendars:
    parameters:
      - $ref: '#/components/parameters/WorldId'
    get:
      tags: [calendars]
      summary: List a world's calendars
      operationId: listCalendars
      responses:
        '200':
          description: Calendars in the world, newest first.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Calendar'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      tags: [calendars]
      summary: Create a calendar
      operationId: createCalendar
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CalendarRequest'
      responses:
        '201':
          description: Calendar created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Calendar'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/calendars/{calendarId}:
    parameters:
      - $ref: '#/components/parameters/WorldId'
      - $ref: '#/components/parameters/CalendarId'
    put:
      tags: [calendars]
      summary: Update a calendar (replaces its months)
      operationId: updateCalendar
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CalendarRequest'
      responses:
        '200':
          description: Calendar updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Calendar'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags: [calendars]
      summary: Delete a calendar
      operationId: deleteCalendar
      responses:
        '204':
          description: Calendar deleted.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/timelines:
    parameters:
      - $ref: '#/components/parameters/WorldId'
    get:
      tags: [timelines]
      summary: List a world's timelines
      operationId: listTimelines
      responses:
        '200':
          description: Timelines in the world, newest first.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Timeline'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      tags: [timelines]
      summary: Create a timeline
      operationId: createTimeline
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TimelineRequest'
      responses:
        '201':
          description: Timeline created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Timeline'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/timelines/{timelineId}:
    parameters:
      - $ref: '#/components/parameters/WorldId'
      - $ref: '#/components/parameters/TimelineId'
    put:
      tags: [timelines]
      summary: Update a timeline
      operationId: updateTimeline
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TimelineRequest'
      responses:
        '200':
          description: Timeline updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Timeline'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags: [timelines]
      summary: Delete a timeline (and its events)
      operationId: deleteTimeline
      responses:
        '204':
          description: Timeline deleted.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/timelines/{timelineId}/events:
    parameters:
      - $ref: '#/components/parameters/WorldId'
      - $ref: '#/components/parameters/TimelineId'
    get:
      tags: [timelines]
      summary: List a timeline's events (chronological)
      operationId: listEvents
      responses:
        '200':
          description: Events ordered by year, month, day.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/TimelineEvent'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      tags: [timelines]
      summary: Add an event to a timeline
      operationId: createEvent
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TimelineEventRequest'
      responses:
        '201':
          description: Event created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TimelineEvent'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/timelines/{timelineId}/events/{eventId}:
    parameters:
      - $ref: '#/components/parameters/WorldId'
      - $ref: '#/components/parameters/TimelineId'
      - $ref: '#/components/parameters/EventId'
    put:
      tags: [timelines]
      summary: Update an event
      operationId: updateEvent
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TimelineEventRequest'
      responses:
        '200':
          description: Event updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TimelineEvent'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags: [timelines]
      summary: Delete an event
      operationId: deleteEvent
      responses:
        '204':
          description: Event deleted.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/campaigns:
    parameters:
      - $ref: '#/components/parameters/WorldId'
    get:
      tags: [campaigns]
      summary: List a world's campaigns
      operationId: listCampaigns
      responses:
        '200':
          description: Campaigns in the world, newest first.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Campaign'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      tags: [campaigns]
      summary: Create a campaign
      operationId: createCampaign
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CampaignRequest'
      responses:
        '201':
          description: Campaign created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Campaign'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/campaigns/{campaignId}:
    parameters:
      - $ref: '#/components/parameters/WorldId'
      - $ref: '#/components/parameters/CampaignId'
    get:
      tags: [campaigns]
      summary: Get a campaign
      operationId: getCampaign
      responses:
        '200':
          description: The campaign.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Campaign'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      tags: [campaigns]
      summary: Update a campaign
      operationId: updateCampaign
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CampaignRequest'
      responses:
        '200':
          description: Campaign updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Campaign'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags: [campaigns]
      summary: Delete a campaign (and its sessions and arcs)
      operationId: deleteCampaign
      responses:
        '204':
          description: Campaign deleted.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/campaigns/{campaignId}/sessions:
    parameters:
      - $ref: '#/components/parameters/WorldId'
      - $ref: '#/components/parameters/CampaignId'
    get:
      tags: [campaigns]
      summary: List a campaign's sessions (ordered)
      operationId: listSessions
      responses:
        '200':
          description: Sessions ordered by number then date.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Session'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      tags: [campaigns]
      summary: Add a session to a campaign
      operationId: createSession
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SessionRequest'
      responses:
        '201':
          description: Session created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Session'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/campaigns/{campaignId}/sessions/{sessionId}:
    parameters:
      - $ref: '#/components/parameters/WorldId'
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/SessionId'
    put:
      tags: [campaigns]
      summary: Update a session
      operationId: updateSession
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SessionRequest'
      responses:
        '200':
          description: Session updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Session'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags: [campaigns]
      summary: Delete a session
      operationId: deleteSession
      responses:
        '204':
          description: Session deleted.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /dice/roll:
    post:
      tags: [dice]
      summary: Roll a dice expression (e.g. 2d6+3, 2d20kh1)
      operationId: rollDice
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DiceRollRequest'
      responses:
        '200':
          description: The roll result.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DiceRollResult'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /sheet-templates/builtin:
    get:
      tags: [sheets]
      summary: List built-in starter sheet templates (reference data)
      operationId: listBuiltinSheetTemplates
      responses:
        '200':
          description: The built-in templates.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/BuiltinSheetTemplate'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /worlds/{worldId}/sheet-templates:
    parameters:
      - $ref: '#/components/parameters/WorldId'
    get:
      tags: [sheets]
      summary: List a world's sheet templates
      operationId: listSheetTemplates
      responses:
        '200':
          description: Sheet templates in the world, newest first.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/SheetTemplate'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      tags: [sheets]
      summary: Create a sheet template
      operationId: createSheetTemplate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SheetTemplateRequest'
      responses:
        '201':
          description: Template created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SheetTemplate'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/sheet-templates/{templateId}:
    parameters:
      - $ref: '#/components/parameters/WorldId'
      - $ref: '#/components/parameters/SheetTemplateId'
    get:
      tags: [sheets]
      summary: Get a sheet template
      operationId: getSheetTemplate
      responses:
        '200':
          description: The template.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SheetTemplate'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      tags: [sheets]
      summary: Update a sheet template
      operationId: updateSheetTemplate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SheetTemplateRequest'
      responses:
        '200':
          description: Template updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SheetTemplate'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags: [sheets]
      summary: Delete a sheet template
      operationId: deleteSheetTemplate
      responses:
        '204':
          description: Template deleted.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/whiteboards:
    parameters:
      - $ref: '#/components/parameters/WorldId'
    get:
      tags: [whiteboards]
      summary: List a world's whiteboards
      operationId: listWhiteboards
      responses:
        '200':
          description: Whiteboards in the world, newest first.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Whiteboard'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      tags: [whiteboards]
      summary: Create a whiteboard
      operationId: createWhiteboard
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WhiteboardRequest'
      responses:
        '201':
          description: Whiteboard created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Whiteboard'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/whiteboards/{whiteboardId}:
    parameters:
      - $ref: '#/components/parameters/WorldId'
      - $ref: '#/components/parameters/WhiteboardId'
    get:
      tags: [whiteboards]
      summary: Get a whiteboard
      operationId: getWhiteboard
      responses:
        '200':
          description: The whiteboard.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Whiteboard'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      tags: [whiteboards]
      summary: Update a whiteboard (replaces nodes and edges)
      operationId: updateWhiteboard
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WhiteboardRequest'
      responses:
        '200':
          description: Whiteboard updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Whiteboard'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags: [whiteboards]
      summary: Delete a whiteboard
      operationId: deleteWhiteboard
      responses:
        '204':
          description: Whiteboard deleted.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/export:
    parameters:
      - $ref: '#/components/parameters/WorldId'
    get:
      tags: [export]
      summary: Export the whole world as a JSON bundle (downloadable)
      operationId: exportWorld
      responses:
        '200':
          description: A self-describing JSON bundle of every entity in the world.
          content:
            application/json:
              schema:
                type: object
                description: >
                  Keyed collections (world, articles, categories, maps, mapPins,
                  timelines, timelineEvents, calendars, calendarMonths,
                  relationships, campaigns, sessions, arcs, beats, sheetTemplates,
                  characterSheets, statblocks) plus exportVersion/exportedAt.
                additionalProperties: true
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/statblocks:
    parameters:
      - $ref: '#/components/parameters/WorldId'
    get:
      tags: [statblocks]
      summary: List a world's statblocks
      operationId: listStatblocks
      parameters:
        - name: campaignId
          in: query
          required: false
          description: Filter to a single campaign's statblocks.
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Statblocks in the world, newest first.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Statblock'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      tags: [statblocks]
      summary: Create a statblock
      operationId: createStatblock
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StatblockRequest'
      responses:
        '201':
          description: Statblock created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Statblock'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/statblocks/{statblockId}:
    parameters:
      - $ref: '#/components/parameters/WorldId'
      - $ref: '#/components/parameters/StatblockId'
    get:
      tags: [statblocks]
      summary: Get a statblock
      operationId: getStatblock
      responses:
        '200':
          description: The statblock.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Statblock'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      tags: [statblocks]
      summary: Update a statblock
      operationId: updateStatblock
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StatblockRequest'
      responses:
        '200':
          description: Statblock updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Statblock'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags: [statblocks]
      summary: Delete a statblock
      operationId: deleteStatblock
      responses:
        '204':
          description: Statblock deleted.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/character-sheets:
    parameters:
      - $ref: '#/components/parameters/WorldId'
    get:
      tags: [sheets]
      summary: List a world's character sheets
      operationId: listCharacterSheets
      parameters:
        - name: campaignId
          in: query
          required: false
          description: Filter to a single campaign's party.
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Character sheets in the world, newest first.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/CharacterSheet'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      tags: [sheets]
      summary: Create a character sheet
      operationId: createCharacterSheet
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CharacterSheetRequest'
      responses:
        '201':
          description: Sheet created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CharacterSheet'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/character-sheets/{sheetId}:
    parameters:
      - $ref: '#/components/parameters/WorldId'
      - $ref: '#/components/parameters/SheetId'
    get:
      tags: [sheets]
      summary: Get a character sheet
      operationId: getCharacterSheet
      responses:
        '200':
          description: The sheet.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CharacterSheet'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      tags: [sheets]
      summary: Update a character sheet
      operationId: updateCharacterSheet
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CharacterSheetRequest'
      responses:
        '200':
          description: Sheet updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CharacterSheet'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags: [sheets]
      summary: Delete a character sheet
      operationId: deleteCharacterSheet
      responses:
        '204':
          description: Sheet deleted.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/character-sheets/{sheetId}/pdf:
    parameters:
      - $ref: '#/components/parameters/WorldId'
      - $ref: '#/components/parameters/SheetId'
    get:
      tags: [sheets]
      summary: Export a character sheet as a filled fillable PDF (D&D 5e only)
      operationId: exportCharacterSheetPdf
      responses:
        '200':
          description: The filled PDF (still fillable).
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/campaigns/{campaignId}/arcs:
    parameters:
      - $ref: '#/components/parameters/WorldId'
      - $ref: '#/components/parameters/CampaignId'
    get:
      tags: [arcs]
      summary: List a campaign's arcs (ordered)
      operationId: listArcs
      responses:
        '200':
          description: Arcs ordered by position.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Arc'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      tags: [arcs]
      summary: Create an arc
      operationId: createArc
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ArcRequest'
      responses:
        '201':
          description: Arc created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Arc'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/campaigns/{campaignId}/arcs/{arcId}:
    parameters:
      - $ref: '#/components/parameters/WorldId'
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/ArcId'
    put:
      tags: [arcs]
      summary: Update an arc
      operationId: updateArc
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ArcRequest'
      responses:
        '200':
          description: Arc updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Arc'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags: [arcs]
      summary: Delete an arc (and its beats)
      operationId: deleteArc
      responses:
        '204':
          description: Arc deleted.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/campaigns/{campaignId}/arcs/{arcId}/beats:
    parameters:
      - $ref: '#/components/parameters/WorldId'
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/ArcId'
    get:
      tags: [arcs]
      summary: List an arc's beats (ordered)
      operationId: listBeats
      responses:
        '200':
          description: Beats ordered by position.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Beat'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      tags: [arcs]
      summary: Add a beat to an arc
      operationId: createBeat
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BeatRequest'
      responses:
        '201':
          description: Beat created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Beat'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/campaigns/{campaignId}/arcs/{arcId}/beats/{beatId}:
    parameters:
      - $ref: '#/components/parameters/WorldId'
      - $ref: '#/components/parameters/CampaignId'
      - $ref: '#/components/parameters/ArcId'
      - $ref: '#/components/parameters/BeatId'
    put:
      tags: [arcs]
      summary: Update a beat
      operationId: updateBeat
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BeatRequest'
      responses:
        '200':
          description: Beat updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Beat'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags: [arcs]
      summary: Delete a beat
      operationId: deleteBeat
      responses:
        '204':
          description: Beat deleted.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/categories:
    parameters:
      - $ref: '#/components/parameters/WorldId'
    get:
      tags: [categories]
      summary: List a world's categories
      operationId: listCategories
      responses:
        '200':
          description: Categories in the world, ordered by name.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Category'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      tags: [categories]
      summary: Create a category
      operationId: createCategory
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CategoryRequest'
      responses:
        '201':
          description: Category created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Category'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/categories/{categoryId}:
    parameters:
      - $ref: '#/components/parameters/WorldId'
      - $ref: '#/components/parameters/CategoryId'
    put:
      tags: [categories]
      summary: Update a category
      operationId: updateCategory
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CategoryRequest'
      responses:
        '200':
          description: Category updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Category'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags: [categories]
      summary: Delete a category (its articles are kept, uncategorized)
      operationId: deleteCategory
      responses:
        '204':
          description: Category deleted.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/articles:
    parameters:
      - $ref: '#/components/parameters/WorldId'
    get:
      tags: [articles]
      summary: List a world's articles
      operationId: listArticles
      parameters:
        - name: categoryId
          in: query
          required: false
          description: Filter to a single category.
          schema:
            type: string
            format: uuid
        - name: q
          in: query
          required: false
          description: Full-text search query over title and body.
          schema:
            type: string
      responses:
        '200':
          description: Matching articles (summaries), newest first.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ArticleSummary'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    post:
      tags: [articles]
      summary: Create an article
      operationId: createArticle
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ArticleRequest'
      responses:
        '201':
          description: Article created.
          headers:
            Location:
              schema:
                type: string
                format: uri
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Article'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/articles/{articleId}/revisions:
    parameters:
      - $ref: '#/components/parameters/WorldId'
      - $ref: '#/components/parameters/ArticleId'
    get:
      tags: [articles]
      summary: List an article's revisions (newest first)
      operationId: listArticleRevisions
      responses:
        '200':
          description: Snapshots of the article's prior states.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ArticleRevision'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/articles/{articleId}/revisions/{revisionId}/restore:
    parameters:
      - $ref: '#/components/parameters/WorldId'
      - $ref: '#/components/parameters/ArticleId'
      - $ref: '#/components/parameters/RevisionId'
    post:
      tags: [articles]
      summary: Restore the article to a prior revision
      operationId: restoreArticleRevision
      responses:
        '200':
          description: The article after restoring (current state snapshotted first).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Article'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

  /worlds/{worldId}/articles/{articleId}:
    parameters:
      - $ref: '#/components/parameters/WorldId'
      - $ref: '#/components/parameters/ArticleId'
    get:
      tags: [articles]
      summary: Get a single article
      operationId: getArticle
      responses:
        '200':
          description: The article, with rendered (auto-linked) body.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Article'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    put:
      tags: [articles]
      summary: Update an article
      operationId: updateArticle
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ArticleRequest'
      responses:
        '200':
          description: Article updated.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Article'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
    delete:
      tags: [articles]
      summary: Delete an article
      operationId: deleteArticle
      responses:
        '204':
          description: Article deleted.
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: >
        Bearer token obtained from POST /auth/login. Signed with HS256.

  parameters:
    WorldId:
      name: worldId
      in: path
      required: true
      description: Identifier of the world.
      schema:
        type: string
        format: uuid
    CategoryId:
      name: categoryId
      in: path
      required: true
      description: Identifier of the category.
      schema:
        type: string
        format: uuid
    ArticleId:
      name: articleId
      in: path
      required: true
      description: Identifier of the article.
      schema:
        type: string
        format: uuid
    RevisionId:
      name: revisionId
      in: path
      required: true
      description: Identifier of the article revision.
      schema:
        type: string
        format: uuid
    MediaId:
      name: mediaId
      in: path
      required: true
      description: Identifier of the media asset.
      schema:
        type: string
        format: uuid
    CalendarId:
      name: calendarId
      in: path
      required: true
      description: Identifier of the calendar.
      schema:
        type: string
        format: uuid
    RelationshipId:
      name: relationshipId
      in: path
      required: true
      description: Identifier of the relationship.
      schema:
        type: string
        format: uuid
    CampaignId:
      name: campaignId
      in: path
      required: true
      description: Identifier of the campaign.
      schema:
        type: string
        format: uuid
    SessionId:
      name: sessionId
      in: path
      required: true
      description: Identifier of the session.
      schema:
        type: string
        format: uuid
    SheetTemplateId:
      name: templateId
      in: path
      required: true
      description: Identifier of the sheet template.
      schema:
        type: string
        format: uuid
    SheetId:
      name: sheetId
      in: path
      required: true
      description: Identifier of the character sheet.
      schema:
        type: string
        format: uuid
    StatblockId:
      name: statblockId
      in: path
      required: true
      description: Identifier of the statblock.
      schema:
        type: string
        format: uuid
    WhiteboardId:
      name: whiteboardId
      in: path
      required: true
      description: Identifier of the whiteboard.
      schema:
        type: string
        format: uuid
    ArcId:
      name: arcId
      in: path
      required: true
      description: Identifier of the arc.
      schema:
        type: string
        format: uuid
    BeatId:
      name: beatId
      in: path
      required: true
      description: Identifier of the beat.
      schema:
        type: string
        format: uuid
    TimelineId:
      name: timelineId
      in: path
      required: true
      description: Identifier of the timeline.
      schema:
        type: string
        format: uuid
    EventId:
      name: eventId
      in: path
      required: true
      description: Identifier of the event.
      schema:
        type: string
        format: uuid
    MapId:
      name: mapId
      in: path
      required: true
      description: Identifier of the map.
      schema:
        type: string
        format: uuid
    PinId:
      name: pinId
      in: path
      required: true
      description: Identifier of the pin.
      schema:
        type: string
        format: uuid

  responses:
    BadRequest:
      description: The request was malformed or failed validation.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetail'
    Unauthorized:
      description: Missing or invalid bearer token / password.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetail'
    NotFound:
      description: The requested resource does not exist.
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/ProblemDetail'

  schemas:
    DiceRollRequest:
      type: object
      required: [expression]
      properties:
        expression:
          type: string
          minLength: 1
          maxLength: 200
          examples: ["2d6+3"]

    DieRoll:
      type: object
      required: [sides, value, kept]
      properties:
        sides:
          type: integer
        value:
          type: integer
        kept:
          type: boolean
          description: False for dice dropped by keep-highest/lowest.

    DiceRollResult:
      type: object
      required: [expression, total, rolls, breakdown]
      properties:
        expression:
          type: string
        total:
          type: integer
        rolls:
          type: array
          items:
            $ref: '#/components/schemas/DieRoll'
        breakdown:
          type: string
          description: Human-readable summary, e.g. "2d6 [4, 5] + 3 = 12".

    SheetFieldType:
      type: string
      enum: [TEXT, TEXTAREA, NUMBER, BOOLEAN, SELECT, CIRCLES]

    SheetField:
      type: object
      required: [key, label, type]
      properties:
        key:
          type: string
          description: Stable identifier used as the key in a sheet's values.
        label:
          type: string
        type:
          $ref: '#/components/schemas/SheetFieldType'
        options:
          type: [array, "null"]
          items:
            type: string
          description: Choices for a SELECT field.
        width:
          type: [string, "null"]
          enum: [FULL, HALF, THIRD, QUARTER, null]
          description: Column width for side-by-side layout; defaults to FULL.
        count:
          type: [integer, "null"]
          minimum: 1
          description: Number of circles for a CIRCLES field.

    SheetSection:
      type: object
      required: [title, fields]
      properties:
        title:
          type: string
        fields:
          type: array
          items:
            $ref: '#/components/schemas/SheetField'

    SheetTemplateRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
        system:
          type: [string, "null"]
          maxLength: 100
        sections:
          type: array
          items:
            $ref: '#/components/schemas/SheetSection'

    SheetTemplate:
      type: object
      required: [id, worldId, name, sections, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        worldId:
          type: string
          format: uuid
        name:
          type: string
        system:
          type: [string, "null"]
        sections:
          type: array
          items:
            $ref: '#/components/schemas/SheetSection'
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    BuiltinSheetTemplate:
      type: object
      required: [name, sections]
      properties:
        name:
          type: string
        system:
          type: [string, "null"]
        sections:
          type: array
          items:
            $ref: '#/components/schemas/SheetSection'

    WhiteboardNode:
      type: object
      required: [id, text, x, y]
      properties:
        id:
          type: string
        text:
          type: string
        x:
          type: number
          format: double
        y:
          type: number
          format: double
        color:
          type: [string, "null"]

    WhiteboardEdge:
      type: object
      required: [id, fromNodeId, toNodeId]
      properties:
        id:
          type: string
        fromNodeId:
          type: string
        toNodeId:
          type: string
        label:
          type: [string, "null"]

    WhiteboardRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
        nodes:
          type: array
          items:
            $ref: '#/components/schemas/WhiteboardNode'
        edges:
          type: array
          items:
            $ref: '#/components/schemas/WhiteboardEdge'

    Whiteboard:
      type: object
      required: [id, worldId, name, nodes, edges, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        worldId:
          type: string
          format: uuid
        name:
          type: string
        nodes:
          type: array
          items:
            $ref: '#/components/schemas/WhiteboardNode'
        edges:
          type: array
          items:
            $ref: '#/components/schemas/WhiteboardEdge'
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    StatblockRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
        articleId:
          type: [string, "null"]
          format: uuid
        campaignId:
          type: [string, "null"]
          format: uuid
          description: Optional campaign this statblock belongs to; null = shared.
        stats:
          type: object
          additionalProperties: true
          description: Freeform key/value stats (e.g. AC, HP, speed).
        notes:
          type: [string, "null"]
          maxLength: 20000

    Statblock:
      type: object
      required: [id, worldId, name, stats, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        worldId:
          type: string
          format: uuid
        articleId:
          type: [string, "null"]
          format: uuid
        campaignId:
          type: [string, "null"]
          format: uuid
        name:
          type: string
        stats:
          type: object
          additionalProperties: true
        notes:
          type: [string, "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CharacterSheetRequest:
      type: object
      required: [name, templateId]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
        templateId:
          type: string
          format: uuid
        articleId:
          type: [string, "null"]
          format: uuid
          description: Optional linked article (the character's wiki page).
        campaignId:
          type: [string, "null"]
          format: uuid
          description: Optional campaign this character belongs to; null = shared.
        values:
          type: object
          additionalProperties: true
          description: Field values keyed by the template's field keys.

    CharacterSheet:
      type: object
      required: [id, worldId, templateId, name, values, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        worldId:
          type: string
          format: uuid
        templateId:
          type: string
          format: uuid
        articleId:
          type: [string, "null"]
          format: uuid
        campaignId:
          type: [string, "null"]
          format: uuid
        name:
          type: string
        values:
          type: object
          additionalProperties: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    ArcStatus:
      type: string
      enum: [PLANNED, ACTIVE, COMPLETED, ABANDONED]

    ArcRequest:
      type: object
      required: [title]
      properties:
        title:
          type: string
          minLength: 1
          maxLength: 200
        description:
          type: [string, "null"]
          maxLength: 5000
        status:
          $ref: '#/components/schemas/ArcStatus'
        position:
          type: [integer, "null"]
          description: Ordering index within the campaign.

    Arc:
      type: object
      required: [id, campaignId, title, status, position, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        campaignId:
          type: string
          format: uuid
        title:
          type: string
        description:
          type: [string, "null"]
        status:
          $ref: '#/components/schemas/ArcStatus'
        position:
          type: integer
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    BeatRequest:
      type: object
      required: [title]
      properties:
        title:
          type: string
          minLength: 1
          maxLength: 200
        body:
          type: [string, "null"]
          maxLength: 20000
        done:
          type: boolean
          default: false
        articleIds:
          type: array
          items:
            type: string
            format: uuid
          description: Linked articles (place, NPC, item, …).
        sessionId:
          type: [string, "null"]
          format: uuid
          description: Optional session where this beat occurred.
        position:
          type: [integer, "null"]

    Beat:
      type: object
      required: [id, arcId, title, done, articleIds, position, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        arcId:
          type: string
          format: uuid
        title:
          type: string
        body:
          type: [string, "null"]
        done:
          type: boolean
        articleIds:
          type: array
          items:
            type: string
            format: uuid
        sessionId:
          type: [string, "null"]
          format: uuid
        position:
          type: integer
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CampaignRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
        description:
          type: [string, "null"]
          maxLength: 5000
        notes:
          type: [string, "null"]
          maxLength: 20000
          description: Free-text GM notes for the campaign.

    Campaign:
      type: object
      required: [id, worldId, name, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        worldId:
          type: string
          format: uuid
        name:
          type: string
        description:
          type: [string, "null"]
        notes:
          type: [string, "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    SessionRequest:
      type: object
      required: [title]
      properties:
        title:
          type: string
          minLength: 1
          maxLength: 200
        sessionNumber:
          type: [integer, "null"]
          description: Ordering index within the campaign.
        date:
          type: [string, "null"]
          format: date
          description: Real-world date the session was played.
        summary:
          type: [string, "null"]
          maxLength: 20000
        notes:
          type: [string, "null"]
          maxLength: 20000
          description: Private GM notes for the session.

    Session:
      type: object
      required: [id, campaignId, title, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        campaignId:
          type: string
          format: uuid
        title:
          type: string
        sessionNumber:
          type: [integer, "null"]
        date:
          type: [string, "null"]
          format: date
        summary:
          type: [string, "null"]
        notes:
          type: [string, "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    LoginRequest:
      type: object
      required: [password]
      properties:
        password:
          type: string
          minLength: 1
          description: The configured application password.
          examples: ["changeme"]

    TokenResponse:
      type: object
      required: [token, tokenType, expiresAt]
      properties:
        token:
          type: string
          description: "Signed JWT to send as `Authorization: Bearer <token>`."
        tokenType:
          type: string
          const: Bearer
        expiresAt:
          type: string
          format: date-time
          description: Expiry instant of the token (UTC).

    WorldRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
          examples: ["Aethelgard"]
        description:
          type: string
          maxLength: 5000
          description: Optional summary of the world.

    World:
      type: object
      required: [id, name, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        description:
          type: [string, "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CategoryRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
        parentId:
          type: [string, "null"]
          format: uuid
          description: Parent category for nesting, or null for a root category.

    Category:
      type: object
      required: [id, worldId, name, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        worldId:
          type: string
          format: uuid
        parentId:
          type: [string, "null"]
          format: uuid
        name:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    ArticleTemplate:
      type: string
      description: The kind of article; drives structured prompts later.
      enum: [GENERIC, CHARACTER, LOCATION, ORGANIZATION, SPECIES, ITEM, EVENT]

    TemplateSection:
      type: object
      required: [heading, hint]
      properties:
        heading:
          type: string
        hint:
          type: string

    ArticleTemplateInfo:
      type: object
      required: [template, label, sections]
      properties:
        template:
          $ref: '#/components/schemas/ArticleTemplate'
        label:
          type: string
        sections:
          type: array
          items:
            $ref: '#/components/schemas/TemplateSection'

    ArticleRequest:
      type: object
      required: [title]
      properties:
        title:
          type: string
          minLength: 1
          maxLength: 200
        slug:
          type: string
          maxLength: 220
          description: >
            Optional URL-safe identifier, unique per world. Generated from the
            title (and de-duplicated) when omitted.
        template:
          $ref: '#/components/schemas/ArticleTemplate'
        categoryId:
          type: [string, "null"]
          format: uuid
        body:
          type: string
          description: Article body as HTML (see ADR-0013).

    ArticleSummary:
      type: object
      required: [id, worldId, title, slug, template, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        worldId:
          type: string
          format: uuid
        categoryId:
          type: [string, "null"]
          format: uuid
        title:
          type: string
        slug:
          type: string
        template:
          $ref: '#/components/schemas/ArticleTemplate'
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    Article:
      allOf:
        - $ref: '#/components/schemas/ArticleSummary'
        - type: object
          required: [body]
          properties:
            body:
              type: [string, "null"]
              description: Raw article body as HTML, including `[[target]]` wiki-links.
            bodyHtml:
              type: [string, "null"]
              readOnly: true
              description: >
                Rendered body with wiki-links resolved to anchors (see
                ADR-0014). Read-only; derived from `body`.

    ArticleRevision:
      type: object
      required: [id, articleId, title, slug, template, createdAt]
      properties:
        id:
          type: string
          format: uuid
        articleId:
          type: string
          format: uuid
        title:
          type: string
        slug:
          type: string
        template:
          $ref: '#/components/schemas/ArticleTemplate'
        body:
          type: [string, "null"]
        createdAt:
          type: string
          format: date-time

    RelationshipRequest:
      type: object
      required: [fromArticleId, toArticleId]
      properties:
        fromArticleId:
          type: string
          format: uuid
        toArticleId:
          type: string
          format: uuid
        label:
          type: [string, "null"]
          maxLength: 100
          description: Free-text relationship, e.g. "parent of", "allied with".
        directed:
          type: boolean
          default: true
          description: When true, the link has a direction from -> to.

    Relationship:
      type: object
      required: [id, worldId, fromArticleId, toArticleId, directed, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        worldId:
          type: string
          format: uuid
        fromArticleId:
          type: string
          format: uuid
        toArticleId:
          type: string
          format: uuid
        label:
          type: [string, "null"]
        directed:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CalendarMonthInput:
      type: object
      required: [name, days]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 100
        days:
          type: integer
          minimum: 1

    CalendarMonth:
      type: object
      required: [name, days]
      properties:
        name:
          type: string
        days:
          type: integer

    CalendarRequest:
      type: object
      required: [name, months]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
        daysPerWeek:
          type: [integer, "null"]
          minimum: 1
        months:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/CalendarMonthInput'

    Calendar:
      type: object
      required: [id, worldId, name, months, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        worldId:
          type: string
          format: uuid
        name:
          type: string
        daysPerWeek:
          type: [integer, "null"]
        months:
          type: array
          items:
            $ref: '#/components/schemas/CalendarMonth'
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    TimelineRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
        description:
          type: [string, "null"]
          maxLength: 5000
        calendarId:
          type: [string, "null"]
          format: uuid
          description: Optional calendar used to render and validate event dates.

    Timeline:
      type: object
      required: [id, worldId, name, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        worldId:
          type: string
          format: uuid
        name:
          type: string
        description:
          type: [string, "null"]
        calendarId:
          type: [string, "null"]
          format: uuid
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    TimelineEventRequest:
      type: object
      required: [title, year]
      properties:
        title:
          type: string
          minLength: 1
          maxLength: 200
        description:
          type: [string, "null"]
          maxLength: 5000
        articleId:
          type: [string, "null"]
          format: uuid
        year:
          type: integer
          description: Integer year; negative values denote pre-epoch dates.
        month:
          type: [integer, "null"]
          minimum: 1
        day:
          type: [integer, "null"]
          minimum: 1

    TimelineEvent:
      type: object
      required: [id, timelineId, title, year, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        timelineId:
          type: string
          format: uuid
        articleId:
          type: [string, "null"]
          format: uuid
        title:
          type: string
        description:
          type: [string, "null"]
        year:
          type: integer
        month:
          type: [integer, "null"]
        day:
          type: [integer, "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    MapRequest:
      type: object
      required: [name, mediaId]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
        mediaId:
          type: string
          format: uuid
          description: The uploaded image used as the map's base layer.

    Map:
      type: object
      required: [id, worldId, name, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        worldId:
          type: string
          format: uuid
        name:
          type: string
        mediaId:
          type: [string, "null"]
          format: uuid
        imageUrl:
          type: [string, "null"]
          description: Public URL of the base image (null if the image was deleted).
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    MapPinRequest:
      type: object
      required: [x, y]
      properties:
        articleId:
          type: [string, "null"]
          format: uuid
        label:
          type: [string, "null"]
          maxLength: 200
        layer:
          type: [string, "null"]
          maxLength: 100
          description: Optional group used for toggling visibility.
        x:
          type: number
          format: double
          minimum: 0
          maximum: 1
          description: Horizontal position as a fraction of image width.
        y:
          type: number
          format: double
          minimum: 0
          maximum: 1
          description: Vertical position as a fraction of image height.

    MapPin:
      type: object
      required: [id, mapId, x, y, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        mapId:
          type: string
          format: uuid
        articleId:
          type: [string, "null"]
          format: uuid
        label:
          type: [string, "null"]
        layer:
          type: [string, "null"]
        x:
          type: number
          format: double
        y:
          type: number
          format: double
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    MediaAsset:
      type: object
      required: [id, worldId, filename, contentType, size, url, createdAt]
      properties:
        id:
          type: string
          format: uuid
        worldId:
          type: string
          format: uuid
        filename:
          type: string
        contentType:
          type: string
        size:
          type: integer
          format: int64
        url:
          type: string
          description: Public URL serving the bytes.
        createdAt:
          type: string
          format: date-time

    ProblemDetail:
      type: object
      description: RFC 9457 / RFC 7807 problem response.
      properties:
        type:
          type: string
          format: uri
          default: about:blank
        title:
          type: string
        status:
          type: integer
        detail:
          type: string
        instance:
          type: string

security:
  - bearerAuth: []
